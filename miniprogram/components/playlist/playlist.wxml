<view class="p-container" bind:tap="toMusicList">
    <image src="{{playlist.picUrl}}" class="p-image"></image>
    <text class="p-playcount">{{_count}}</text>
    <view class="p-name">{{playlist.name}}</view>
</view>